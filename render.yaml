services:
  - type: web
    name: vfis-agentic-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn vfis.api.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
